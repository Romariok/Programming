package src.Command;

/**
 * The {@code CommandWithBoolean} interface is used to implement the command pattern with boolean as argument
 * @author Roman Kobelev
 */
public interface CommandWithBoolean {
    void execute(boolean bool);
}
